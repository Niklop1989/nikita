name: Demo Workflow
on: workflow_dispatch
jobs:
  print:
    runs-on: ubuntu-latest
    steps: 
      - name: Run my project  
        run: echo Hello run my project
      - name: Print a few lines
        run: |
          echo First line
          echo second line


    
